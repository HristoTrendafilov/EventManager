// eslint-disable-next-line no-shadow
export enum UserProfileEventType {
  Subscriptions = 1,
  Created = 2,
}

// eslint-disable-next-line no-shadow
export enum UserProfileOrganizationType {
  Subscriptions = 1,
  Managed = 2,
}
